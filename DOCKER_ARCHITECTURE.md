# 🏗️ Docker Architecture Diagram for Cardano.org

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        CARDANO.ORG DOCKER SETUP                         │
│                                                                         │
│  Developer Laptop/Workstation (Linux/macOS/Windows)                    │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
                    ▼               ▼               ▼
        ┌─────────────────┐ ┌─────────────┐ ┌─────────────┐
        │  Docker Engine  │ │   Docker    │ │   Docker    │
        │                 │ │   Compose   │ │   BuildKit  │
        │  (Runtime)      │ │ (Orchestr.) │ │  (Builder)  │
        └─────────────────┘ └─────────────┘ └─────────────┘
                                    │
                    ┌───────────────┼───────────────┬────────────┐
                    │               │               │            │
                    ▼               ▼               ▼            ▼
        ┌──────────────────────────────────────────────────────────────┐
        │                    DOCKER SERVICES                           │
        ├──────────────────┬───────────────┬──────────────┬───────────┤
        │  cardano-dev     │ cardano-prod  │ cardano-test │ builder   │
        │  (port 3000)     │ (port 3001)   │ (no ports)   │(artifacts)│
        └──────────────────┴───────────────┴──────────────┴───────────┘
```

## Multi-Stage Dockerfile Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          DOCKERFILE STAGES                              │
└─────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────┐
    │ Stage 1: BASE                                               │
    │ ┌─────────────────────────────────────────────────────────┐ │
    │ │ FROM node:20-alpine                                     │ │
    │ │ • Alpine Linux (minimal, secure)                        │ │
    │ │ • Node.js 20.0 LTS                                      │ │
    │ │ • dumb-init (signal handling)                           │ │
    │ │ • System dependencies (python3, make, g++, git)         │ │
    │ │ • Yarn 1.22.19                                          │ │
    │ │ • WORKDIR /app                                          │ │
    │ └─────────────────────────────────────────────────────────┘ │
    └───────────────────────────┬─────────────────────────────────┘
                                │
                                ├──────────────────────────────────┐
                                ▼                                  │
    ┌─────────────────────────────────────────────────────────────┐│
    │ Stage 2: DEPENDENCIES                                       ││
    │ ┌─────────────────────────────────────────────────────────┐ ││
    │ │ FROM base                                               │ ││
    │ │ • COPY package.json yarn.lock                           │ ││
    │ │ • RUN yarn install --frozen-lockfile                    │ ││
    │ │ • Uses BuildKit cache mount for /root/.yarn             │ ││
    │ │ • Installs ALL dependencies (dev + prod)                │ ││
    │ │ • Result: /app/node_modules (cached layer)              │ ││
    │ └─────────────────────────────────────────────────────────┘ ││
    └───────────────────────────┬─────────────────────────────────┘│
                                │                                  │
                ┌───────────────┼──────────────┬───────────────────┘
                │               │              │
                ▼               ▼              ▼
┌────────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│ Stage 3:           │ │ Stage 4:         │ │ Stage 6:         │
│ DEVELOPMENT        │ │ BUILDER          │ │ TEST             │
│ ┌────────────────┐ │ │ ┌──────────────┐ │ │ ┌──────────────┐ │
│ │ FROM base      │ │ │ │ FROM base    │ │ │ │ FROM base    │ │
│ │ COPY deps      │ │ │ │ COPY deps    │ │ │ │ COPY deps    │ │
│ │ COPY src       │ │ │ │ COPY src     │ │ │ │ COPY src     │ │
│ │ build-authors  │ │ │ │ yarn build   │ │ │ │ yarn build   │ │
│ │ EXPOSE 3000    │ │ │ │ Output:      │ │ │ │ (validation) │ │
│ │ yarn start     │ │ │ │ /app/build/  │ │ │ └──────────────┘ │
│ │ (hot-reload)   │ │ │ └──────────────┘ │ │                  │
│ └────────────────┘ │ └────────┬─────────┘ └──────────────────┘
│                    │          │
│ DEFAULT STAGE      │          │
└────────────────────┘          ▼
                     ┌─────────────────────┐
                     │ Stage 5:            │
                     │ PRODUCTION          │
                     │ ┌─────────────────┐ │
                     │ │ FROM node:20-   │ │
                     │ │      alpine     │ │
                     │ │ Install serve   │ │
                     │ │ COPY build/     │ │
                     │ │ EXPOSE 3000     │ │
                     │ │ serve -s build  │ │
                     │ └─────────────────┘ │
                     │                     │
                     │ MINIMAL SIZE        │
                     │ (~150MB)            │
                     └─────────────────────┘
```

## Data Flow: Development Mode

```
┌──────────────────────────────────────────────────────────────────────┐
│                    DEVELOPMENT WORKFLOW                              │
└──────────────────────────────────────────────────────────────────────┘

Developer's Machine                     Docker Container
┌────────────────────┐                 ┌─────────────────────┐
│                    │                 │                     │
│  Project Files     │                 │  Container FS       │
│  ┌──────────────┐  │   Volume Mount  │  ┌───────────────┐  │
│  │ ./docs/      │─────────────────────▶│  /app/docs/   │  │
│  │ ./blog/      │─────────────────────▶│  /app/blog/   │  │
│  │ ./src/       │─────────────────────▶│  /app/src/    │  │
│  │ ./static/    │─────────────────────▶│  /app/static/ │  │
│  │ *.config.js  │─────────────────────▶│  /app/*.js    │  │
│  └──────────────┘  │                 │  └───────────────┘  │
│                    │                 │                     │
│  NOT Mounted:      │                 │  Container Only:    │
│  ┌──────────────┐  │                 │  ┌───────────────┐  │
│  │ node_modules │  │        ✗        │  │ node_modules  │  │
│  │ .docusaurus  │  │        ✗        │  │ .docusaurus   │  │
│  │ build/       │  │        ✗        │  │ build/        │  │
│  └──────────────┘  │                 │  └───────────────┘  │
│                    │                 │                     │
└────────────────────┘                 └──────────┬──────────┘
                                                  │
                                    ┌─────────────▼──────────────┐
                                    │  Docusaurus Dev Server     │
                                    │  • Watches for changes     │
                                    │  • Hot Module Replacement  │
                                    │  • Port 3000               │
                                    └─────────────┬──────────────┘
                                                  │
                                    ┌─────────────▼──────────────┐
                                    │  Browser Auto-Refresh      │
                                    │  http://localhost:3000     │
                                    └────────────────────────────┘

 Edit File ──▶ Save ──▶ Volume Mount ──▶ Docusaurus Detects ──▶ Browser Refreshes
                                         (<1 second latency)
```

## Service Architecture

```
┌──────────────────────────────────────────────────────────────────────┐
│                    DOCKER COMPOSE SERVICES                           │
└──────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│  cardano-dev        │
│  ┌───────────────┐  │   Features:
│  │ Target: dev   │  │   • Hot-reload enabled
│  │ Port: 3000    │  │   • Volumes mounted
│  │ Restart: auto │  │   • WATCHPACK_POLLING: true
│  │ Health: ✓     │  │   • CHOKIDAR_USEPOLLING: true
│  └───────────────┘  │   • Network: cardano-network
└─────────────────────┘   Use: Daily development

┌─────────────────────┐
│  cardano-prod       │
│  ┌───────────────┐  │   Features:
│  │ Target: prod  │  │   • Optimized build
│  │ Port: 3001    │  │   • Minimal size (~150MB)
│  │ Restart: auto │  │   • serve static files
│  │ Health: ✓     │  │   • No volumes
│  └───────────────┘  │   • Network: cardano-network
└─────────────────────┘   Use: Production preview

┌─────────────────────┐
│  cardano-test       │
│  ┌───────────────┐  │   Features:
│  │ Target: test  │  │   • Build validation
│  │ Port: none    │  │   • Exits after test
│  │ Restart: no   │  │   • Can run any command
│  │ Health: -     │  │   • Network: cardano-network
│  └───────────────┘  │
└─────────────────────┘   Use: CI/CD pipelines

┌─────────────────────┐
│  cardano-builder    │
│  ┌───────────────┐  │   Features:
│  │ Target: build │  │   • Generates artifacts
│  │ Port: none    │  │   • Outputs to ./build
│  │ Restart: no   │  │   • Volume: ./build
│  │ Health: -     │  │   • Network: cardano-network
│  └───────────────┘  │
└─────────────────────┘   Use: Local builds for deploy
```

## Network Architecture

```
┌──────────────────────────────────────────────────────────────────────┐
│                         NETWORK TOPOLOGY                             │
└──────────────────────────────────────────────────────────────────────┘

Host Machine
┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│  Port 3000 ──▶ [Bridge] ──▶ cardano-dev:3000                      │
│                   │                                                │
│  Port 3001 ──▶ [Bridge] ──▶ cardano-prod:3000                     │
│                   │                                                │
│                   │                                                │
│                   ▼                                                │
│         ┌─────────────────────┐                                   │
│         │  cardano-network    │                                   │
│         │  (bridge driver)    │                                   │
│         │                     │                                   │
│         │  ┌───────────────┐  │                                   │
│         │  │ cardano-dev   │  │                                   │
│         │  └───────────────┘  │                                   │
│         │  ┌───────────────┐  │                                   │
│         │  │ cardano-prod  │  │                                   │
│         │  └───────────────┘  │                                   │
│         │  ┌───────────────┐  │                                   │
│         │  │ cardano-test  │  │                                   │
│         │  └───────────────┘  │                                   │
│         │  ┌───────────────┐  │                                   │
│         │  │ cardano-build │  │                                   │
│         │  └───────────────┘  │                                   │
│         └─────────────────────┘                                   │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

## Build Process Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                      BUILD PROCESS (First Time)                      │
└──────────────────────────────────────────────────────────────────────┘

docker-compose build cardano-dev
        │
        ▼
┌─────────────────────┐
│ Parse Dockerfile    │
│ Load .dockerignore  │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Stage 1: base       │
│ • Pull node:20-alpine (~50MB download)
│ • Install dumb-init
│ • Install system deps (python3, make, g++, git)
│ • Set Yarn version
│ Time: ~30-60s
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Stage 2: deps       │
│ • Copy package.json, yarn.lock
│ • yarn install --frozen-lockfile
│ • Download ~500MB of dependencies
│ Time: ~2-3 minutes
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Stage 3: dev        │
│ • Copy node_modules from deps
│ • Copy source code
│ • yarn build-authors
│ Time: ~10-20s
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Final Image         │
│ • cardano-dev:latest
│ • Size: ~1.0 GB
│ • Ready to run
└─────────────────────┘

Total first build: 3-5 minutes
Cached rebuild: 30-60 seconds (only source code layer rebuilt)
```

## Caching Strategy

```
┌──────────────────────────────────────────────────────────────────────┐
│                        LAYER CACHING                                 │
└──────────────────────────────────────────────────────────────────────┘

Dockerfile Layer              Cache Behavior        Rebuild Frequency
┌─────────────────────────┐  
│ FROM node:20-alpine     │   ✓ CACHED             Once (base image)
├─────────────────────────┤
│ RUN apk add ...         │   ✓ CACHED             Rarely (system deps)
├─────────────────────────┤
│ COPY package.json ...   │   ✓ CACHED             When deps change
├─────────────────────────┤
│ RUN yarn install        │   ✓ CACHED             When deps change
├─────────────────────────┤   (mount cache)        (~5% of builds)
│ COPY . .                │   ✗ REBUILT            Every source change
├─────────────────────────┤   (fast copy)          (~95% of builds)
│ RUN yarn build-authors  │   ✗ REBUILT            Every source change
└─────────────────────────┘   (fast script)

Result: Most builds only rebuild the last 2 layers (fast!)
```

## Volume Mapping Detail

```
┌──────────────────────────────────────────────────────────────────────┐
│                    VOLUME MAPPING (Development)                      │
└──────────────────────────────────────────────────────────────────────┘

Host Path                    Container Path           Type
─────────────────────────────────────────────────────────────────────
./docs/                 →    /app/docs/              bind mount (rw)
./blog/                 →    /app/blog/              bind mount (rw)
./src/                  →    /app/src/               bind mount (rw)
./static/               →    /app/static/            bind mount (rw)
./scripts/              →    /app/scripts/           bind mount (rw)
./docusaurus.config.js  →    /app/docusaurus.config.js  bind mount (rw)
./sidebars.js           →    /app/sidebars.js        bind mount (rw)
./babel.config.js       →    /app/babel.config.js    bind mount (rw)
./variables.js          →    /app/variables.js       bind mount (rw)
./hubspot.config.js     →    /app/hubspot.config.js  bind mount (rw)

(excluded)              →    /app/node_modules       anonymous volume
(excluded)              →    /app/.docusaurus        anonymous volume
(excluded)              →    /app/build              anonymous volume

Benefit: Changes in host immediately visible in container
```

## Cross-Platform Compatibility

```
┌──────────────────────────────────────────────────────────────────────┐
│                    PLATFORM SUPPORT                                  │
└──────────────────────────────────────────────────────────────────────┘

┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│     Linux       │   │     macOS       │   │    Windows      │
│                 │   │                 │   │                 │
│ • Native Docker │   │ • Docker Desktop│   │ • Docker Desktop│
│ • Best perf.    │   │ • HyperKit VM   │   │ • WSL2 backend  │
│ • Direct access │   │ • 95% perf.     │   │ • 90% perf.     │
│                 │   │                 │   │                 │
│ Architecture:   │   │ Architecture:   │   │ Architecture:   │
│ • x86_64        │   │ • x86_64 (Intel)│   │ • x86_64        │
│ • ARM64         │   │ • ARM64 (M1/M2) │   │ • ARM64 (rare)  │
└─────────────────┘   └─────────────────┘   └─────────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                              ▼
                  ┌───────────────────────┐
                  │   Multi-arch Image    │
                  │ • linux/amd64         │
                  │ • linux/arm64         │
                  │ Auto-selected by CPU  │
                  └───────────────────────┘
```

## Comparison: Local vs Docker

```
┌──────────────────────────────────────────────────────────────────────┐
│                    SETUP COMPARISON                                  │
└──────────────────────────────────────────────────────────────────────┘

Local Installation                Docker Installation
┌────────────────────────┐        ┌────────────────────────┐
│ 1. Install Node.js 20  │        │ 1. Install Docker      │
│ 2. Install Yarn 1.22   │        │ 2. Clone repo          │
│ 3. Install Xcode (Mac) │        │ 3. docker-compose up   │
│ 4. Clone repo          │        │                        │
│ 5. yarn install        │        │ Done! ✓                │
│ 6. yarn start          │        │                        │
│                        │        │ Time: 2 min first run  │
│ Time: 10-20 minutes    │        │       5s subsequent    │
└────────────────────────┘        └────────────────────────┘

Issues:                           Benefits:
• Version conflicts               • Isolated environment
• OS-specific problems            • Consistent across team
• Global dependency mess          • No local Node needed
• Different on each machine       • Clean uninstall

Recommendation: Use Docker for development
```

## File Structure

```
cardano-org/
│
├── Dockerfile                    ← Multi-stage build (6 stages)
├── docker-compose.yml            ← Service orchestration (4 services)
├── .dockerignore                 ← Build context optimization
│
├── DOCKER_QUICKSTART.md          ← 2-minute quick start guide
├── DOCKER_STRATEGY.md            ← Technical strategy document
├── DOCKER_IMPLEMENTATION_SUMMARY.md ← This complete summary
│
├── README.md                     ← Updated with Docker section
│   └── "Running Developer Experience in Docker" (line 86-577)
│
└── [existing project files...]
```

---

**Last Updated:** December 1, 2025  
**Diagram Version:** 1.0  
**Status:** ✅ Production Ready
